-- radien
local r = 4

-- mitt punkten av cirklen
local y0 = 0
local x0 = 0
local z0 = 0

-- antalet pungter
local i = 11

-- en pungt på cirklen rand
y =
x =
z =

-- beräkna max y, x, z
-- r^2 = (y - y0)^2 + (x - x0)^2 + (z - z0)^2


-- genererar random pungter
local p_generation = function
  local k = {}
  local var = false
  while var = false do
    local y = math.random(0, r)
    local x = math.random(0, r)
    local z = math.random(0, r)
    if r^2 = (y - y0)^2 + (x - x0)^2 + (z - z0)^2 then
      var = true
    end
  end
end


while  do
  -- body...
end
